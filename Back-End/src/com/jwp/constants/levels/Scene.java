package com.jwp.constants.levels;


/**
 * The top level.
 * Every scene contains multiple label objects which in turn contain may contain 1 or more choices
 * which intern contain 2 or more options.
 * The scene level is necessary as each label has to be unique at the scene level but can be reused between scenes
 * so the program has to be able to distinguish between them.
 */
public class Scene {
}
